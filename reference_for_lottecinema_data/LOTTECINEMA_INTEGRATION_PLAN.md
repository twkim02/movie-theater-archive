# 🎬 롯데시네마 상영 정보 통합 작업 계획서

이 문서는 무비어리 앱에 롯데시네마 상영 정보를 통합하는 작업 순서를 정리합니다.

---

## 📋 작업 개요

### 목표
1. 탐색 탭의 '영화관 보기' 버튼 클릭 시, 롯데시네마 영화관에 한해 실제 상영 시간표를 표시
2. TMDb에서 상영 중이 아니지만 롯데시네마에서는 상영 중인 영화를 `isRecent = true`로 설정
3. 롯데시네마가 아닌 영화관은 기존처럼 하이퍼링크 제공

### 주요 고려사항
- **안드로이드 앱 환경**: HTTP 요청이 제한될 수 있음
- **과도한 호출 방지**: 캐싱, 딜레이, 요청 제한 등 필요
- **영화 제목 매칭**: TMDb 제목과 롯데시네마 제목이 다를 수 있음
- **CSV 파일 관리**: 앱에 포함하여 사용 (assets 폴더)

---

## 📝 작업 단계

### 1단계: 데이터 모델 및 CSV 파싱 준비

#### 1.1. CSV 파일을 assets에 추가
- `lottecinema_movie_now.csv` → `assets/lottecinema/movie_now.csv`
- `lottecinema_movie_upcoming.csv` → `assets/lottecinema/movie_upcoming.csv`
- `lottecinema_theater.csv` → `assets/lottecinema/theater.csv`
- `pubspec.yaml`에 assets 경로 추가

#### 1.2. 롯데시네마 데이터 모델 생성
**파일**: `lib/models/lottecinema_data.dart`
- `LotteCinemaMovie`: movieNo, movieName
- `LotteCinemaTheater`: divisionCode, detailDivisionCode, cinemaID, element
- `LotteCinemaSchedule`: 상영 시간표 정보 (startTime, endTime, screenName, etc.)

#### 1.3. CSV 파서 유틸리티 생성
**파일**: `lib/utils/csv_parser.dart`
- CSV 파일을 읽어서 모델로 변환
- 앱 시작 시 한 번만 로드하고 메모리에 캐싱

**테스트 포인트**: CSV 파일이 제대로 파싱되는지 확인

---

### 2단계: 롯데시네마 API 클라이언트 구현

#### 2.1. 롯데시네마 API 클라이언트 생성
**파일**: `lib/api/lottecinema_client.dart`

**주요 기능**:
- `getMovieSchedule()`: 특정 영화관, 특정 영화의 상영 시간표 가져오기
- API 엔드포인트: `https://www.lottecinema.co.kr/LCWS/Ticketing/TicketingData.aspx`
- POST 요청 (MethodName: "GetPlaySequence")
- 필요한 파라미터:
  - `playDate`: YYYY-MM-DD 형식
  - `cinemaID`: "divisionCode|detailDivisionCode|cinemaID" 형식
  - `representationMovieCode`: 영화의 movieNo

**주의사항**:
- User-Agent, Referer, Origin 헤더 필수
- 네트워크 에러 처리
- 타임아웃 설정 (5초)
- 요청 실패 시 빈 리스트 반환 (앱이 멈추지 않도록)

#### 2.2. 영화 제목 매칭 로직
**파일**: `lib/services/movie_title_matcher.dart`

**기능**:
- TMDb 영화 제목과 롯데시네마 영화명을 매칭
- 정확한 매칭 우선, 부분 매칭도 지원
- 예: "아바타: 불과 재" ↔ "아바타: 불과 재" (정확)
- 예: "극장판 주술회전: 시부야사변 X 사멸회유" ↔ "극장판 주술회전: 시부야사변 X 사멸회유" (정확)
- 예: "만약에 우리" ↔ "만약에 우리" (정확)

**매칭 전략**:
1. 정확한 제목 매칭 (대소문자 무시, 공백 정규화)
2. 부분 매칭 (한쪽이 다른 쪽을 포함)
3. 특수문자 제거 후 매칭

**테스트 포인트**: 
- CSV의 영화명과 TMDb 제목이 매칭되는지 확인
- 매칭 실패 시 빈 결과 반환하는지 확인

---

### 3단계: 영화관 정보와 상영 시간표 통합

#### 3.1. Theater 모델 확장 (필요 시)
현재 `Theater` 모델에 `showtimes` 필드가 이미 있으므로 추가 수정 불필요

#### 3.2. 상영 시간표 가져오기 로직
**파일**: `lib/services/theater_schedule_service.dart`

**주요 기능**:
- `getLotteCinemaSchedule()`: 롯데시네마 영화관의 상영 시간표 가져오기
- 영화관 이름에 "롯데시네마"가 포함되어 있는지 확인
- 포함되어 있으면:
  1. 영화 제목으로 롯데시네마 movieNo 찾기
  2. 영화관 정보로 cinemaID 찾기
  3. API 호출하여 상영 시간표 가져오기
  4. `Showtime` 리스트로 변환
- 포함되어 있지 않으면:
  - 빈 `showtimes` 리스트 반환 (기존 로직 유지)

**캐싱 전략**:
- 같은 영화, 같은 영화관, 같은 날짜에 대한 요청은 5분간 캐싱
- `Map<String, List<Showtime>>` 형태로 메모리 캐싱
- 키: "movieNo_cinemaID_date" 형식

**에러 처리**:
- API 호출 실패 시 빈 리스트 반환
- 네트워크 에러는 조용히 처리 (사용자에게는 하이퍼링크만 표시)

#### 3.3. dummy_theaters.dart 수정
**파일**: `lib/data/dummy_theaters.dart`

**수정 내용**:
- `fetchNearbyTheatersReal()` 함수에서 롯데시네마 영화관을 찾으면
- `TheaterScheduleService`를 호출하여 상영 시간표 가져오기
- `showtimes` 필드에 실제 데이터 채우기

**테스트 포인트**:
- 롯데시네마 영화관에서 상영 시간표가 표시되는지 확인
- 다른 영화관은 기존처럼 하이퍼링크만 표시되는지 확인

---

### 4단계: TMDb 초기화 시 롯데시네마 상영 여부 확인

#### 4.1. 롯데시네마 상영 여부 확인 서비스
**파일**: `lib/services/lottecinema_movie_checker.dart`

**주요 기능**:
- `isPlayingInLotteCinema(String movieTitle)`: 영화가 롯데시네마에서 상영 중인지 확인
- CSV 파일 (`movie_now.csv`, `movie_upcoming.csv`)에서 영화명 검색
- 상영 중이면 `true` 반환

#### 4.2. MovieInitializationService 수정
**파일**: `lib/services/movie_initialization_service.dart`

**수정 내용**:
- `_saveNowPlayingMovies()` 함수에서:
  1. TMDb에서 `isRecent = false`로 설정된 영화도
  2. 롯데시네마 CSV에서 상영 중인지 확인
  3. 상영 중이면 `isRecent = true`로 설정
- `_savePopularMovies()` 함수에서도 동일하게 적용

**주의사항**:
- CSV 파일 로딩은 앱 시작 시 한 번만 (캐싱)
- 매칭 실패해도 에러 발생하지 않도록 처리

**테스트 포인트**:
- TMDb에서 상영 중이 아니지만 롯데시네마에서는 상영 중인 영화가 `isRecent = true`로 설정되는지 확인

---

### 5단계: UI 통합 및 최적화

#### 5.1. TheaterCard 위젯 수정 (선택사항)
**파일**: `lib/widgets/theater_card.dart`

**수정 내용**:
- `showtimes`가 비어있지 않으면 상영 시간표 표시
- 비어있으면 기존처럼 하이퍼링크만 표시

**참고**: 스크린 부분은 디자인 중이므로 당장 수정하지 않아도 됨

#### 5.2. 에러 처리 및 사용자 경험 개선
- 네트워크 에러 시 조용히 처리 (하이퍼링크로 fallback)
- 로딩 상태 표시 (선택사항)
- 상영 시간표가 없을 때 안내 메시지 (선택사항)

---

## 🔄 작업 순서 및 테스트 계획

### Phase 1: 기반 구조 (1단계 + 2단계)
**목표**: CSV 파싱 및 API 클라이언트 구현

**작업 내용**:
1. CSV 파일을 assets에 추가
2. 데이터 모델 생성
3. CSV 파서 구현
4. 롯데시네마 API 클라이언트 구현

**테스트**:
- CSV 파일이 제대로 파싱되는지 확인
- API 클라이언트가 정상적으로 요청을 보내는지 확인 (실제 API 호출 테스트)

**예상 소요 시간**: 2-3시간

---

### Phase 2: 상영 시간표 통합 (3단계)
**목표**: 영화관 화면에서 롯데시네마 상영 시간표 표시

**작업 내용**:
1. 영화 제목 매칭 로직 구현
2. 상영 시간표 서비스 구현
3. `dummy_theaters.dart` 수정

**테스트**:
- 롯데시네마 영화관에서 상영 시간표가 표시되는지 확인
- 다른 영화관은 기존처럼 동작하는지 확인
- 네트워크 에러 시 앱이 멈추지 않는지 확인

**예상 소요 시간**: 3-4시간

---

### Phase 3: TMDb 초기화 개선 (4단계)
**목표**: 롯데시네마 상영 영화를 `isRecent = true`로 설정

**작업 내용**:
1. 롯데시네마 상영 여부 확인 서비스 구현
2. `MovieInitializationService` 수정

**테스트**:
- TMDb 초기화 시 롯데시네마 상영 영화가 `isRecent = true`로 설정되는지 확인
- 기존 영화 데이터에 대해서도 동작하는지 확인

**예상 소요 시간**: 2-3시간

---

### Phase 4: 최적화 및 버그 수정 (5단계)
**목표**: 사용자 경험 개선 및 안정성 향상

**작업 내용**:
1. 캐싱 로직 최적화
2. 에러 처리 개선
3. 성능 최적화 (필요 시)

**테스트**:
- 반복 요청 시 캐싱이 제대로 동작하는지 확인
- 다양한 에러 상황에서 앱이 안정적으로 동작하는지 확인

**예상 소요 시간**: 2-3시간

---

## ⚠️ 주의사항

### 1. API 호출 제한
- 롯데시네마 API는 공식 API가 아니므로 호출 제한이 있을 수 있음
- 캐싱을 적극 활용하여 불필요한 호출 방지
- 사용자가 명시적으로 새로고침할 때만 API 호출

### 2. 영화 제목 매칭
- TMDb 제목과 롯데시네마 제목이 완전히 일치하지 않을 수 있음
- 매칭 실패 시 조용히 처리 (하이퍼링크로 fallback)
- 매칭 로직은 점진적으로 개선 가능

### 3. 네트워크 환경
- 안드로이드 앱에서 네트워크가 불안정할 수 있음
- 모든 네트워크 요청에 타임아웃 설정
- 에러 발생 시 사용자에게 명확한 피드백 제공

### 4. CSV 파일 업데이트
- CSV 파일은 수동으로 업데이트해야 함
- 앱 업데이트 시 최신 CSV 파일 포함
- 향후 자동화 가능 (서버에서 CSV 제공)

### 5. 성능
- CSV 파일 로딩은 앱 시작 시 한 번만
- 상영 시간표는 캐싱하여 반복 요청 방지
- 메모리 사용량 모니터링

---

## 📚 참고 자료

### CSV 파일 구조
- `lottecinema_movie_now.csv`: 현재 상영 중인 영화 (movieNo, movieName)
- `lottecinema_movie_upcoming.csv`: 개봉 예정 영화 (movieNo, movieName)
- `lottecinema_theater.csv`: 영화관 정보 (divisionCode, detailDivisionCode, cinemaID, element)

### API 엔드포인트
- URL: `https://www.lottecinema.co.kr/LCWS/Ticketing/TicketingData.aspx`
- Method: POST
- Headers:
  - User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36
  - Referer: https://www.lottecinema.co.kr/NLCHS/Ticketing
  - Origin: https://www.lottecinema.co.kr
  - Content-Type: application/x-www-form-urlencoded
- Body:
  ```json
  {
    "paramList": "{\"MethodName\":\"GetPlaySequence\",\"channelType\":\"HO\",\"osType\":\"W\",\"osVersion\":\"...\",\"playDate\":\"2026-01-13\",\"cinemaID\":\"1|0003|4008\",\"representationMovieCode\":\"23663\"}"
  }
  ```

### 응답 구조
```json
{
  "PlaySeqs": {
    "Items": [
      {
        "MovieNameKR": "만약에 우리",
        "StartTime": "17:30",
        "EndTime": "19:34",
        "ScreenNameKR": "4관",
        "TotalSeatCount": 200,
        "BookingSeatCount": 50
      }
    ]
  }
}
```

---

## ✅ 체크리스트

### Phase 1
- [ ] CSV 파일을 assets에 추가
- [ ] `pubspec.yaml`에 assets 경로 추가
- [ ] 롯데시네마 데이터 모델 생성
- [ ] CSV 파서 구현
- [ ] 롯데시네마 API 클라이언트 구현
- [ ] API 클라이언트 테스트

### Phase 2
- [ ] 영화 제목 매칭 로직 구현
- [ ] 상영 시간표 서비스 구현
- [ ] `dummy_theaters.dart` 수정
- [ ] 롯데시네마 영화관에서 상영 시간표 표시 테스트
- [ ] 다른 영화관 동작 확인

### Phase 3
- [ ] 롯데시네마 상영 여부 확인 서비스 구현
- [ ] `MovieInitializationService` 수정
- [ ] TMDb 초기화 시 롯데시네마 상영 영화 확인 테스트

### Phase 4
- [ ] 캐싱 로직 최적화
- [ ] 에러 처리 개선
- [ ] 성능 테스트
- [ ] 전체 통합 테스트

---

**문서 작성일**: 2026년 1월  
**예상 총 소요 시간**: 9-13시간 (단계별로 나눠서 진행)  
**우선순위**: Phase 1 → Phase 2 → Phase 3 → Phase 4
